# KOVA OS — Component Repository Instructions

This repository is an **older public-site / redirect / documentation layer** for KOVA OS. It is not the canonical orchestration hub and should not declare a Manus-managed repository or any other component as the primary KOVA system.

Canonical orchestration and repository roles live in `Kathrynhiggs21/Kova-ai-SYSTEM` (`AGENTS.md` + `KOVA_REPO_MAP.md`).

Rules:
1. Treat historical statements naming another "Primary Repo" as stale unless the orchestration map records a migration.
2. Prefer migrating useful site/docs content into the active web stack rather than maintaining a second public-site implementation.
3. Do not commit secrets or production credentials.
4. `kovaos.com` is the canonical KOVA domain.
